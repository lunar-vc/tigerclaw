# Environment secrets
.env

# Dependencies
node_modules/

# Claude Code local settings (per-machine)
.claude/settings.local.json

# Cloned skills repo (pulled on flox activate)
.claude/skills/agent-skills/

# MCP auth tokens (generated by /mcp OAuth flow)
.mcp-auth/

# Legacy memory directory (replaced by Claude Code native memory)
.memory/

# Pipeline dedup index (local state, rebuilt from memory)
.pipeline-index.json

# Enrichment cache (local, rebuilt by fetching)
.enrichment-cache/

# FalkorDB graph data (local, rebuilt by backfill-graph.js)
data/graph/

# Query performance log (local analytics)
.query-performance.jsonl

# Session-scoped discovery feed
.discoveries
.discoveries.jsonl
.discoveries-*.jsonl

# Session-scoped themes (populated by Claude via Linear MCP)
.themes

# macOS
.DS_Store

# Research output (large/generated files)
research/*.json
research/*.csv
